/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'images.pexels.com',
            'source.unsplash.com'
        ]
    }
}

module.exports = nextConfig
